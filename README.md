# hankfullanarchymilestone
The Electron app needs electron, so install that using npm init -y Then for electron npm i electron After that you can run node . or main.js and it will start the electron app.
